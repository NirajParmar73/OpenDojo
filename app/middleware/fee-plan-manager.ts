// Authorization is enforced by the fee-plan API endpoints. Do not block this
// page from the client: role assignments can be nested and may not be loaded
// when route middleware runs.
export default defineNuxtRouteMiddleware(() => {})
